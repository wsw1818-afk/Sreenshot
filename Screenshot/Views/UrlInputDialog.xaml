<Window x:Class="Screenshot.Views.UrlInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chrome 전체 페이지 캡처"
        Height="200" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1e1e1e">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="캡처할 웹페이지 URL을 입력하세요:"
                   Foreground="White"
                   FontSize="14"
                   Margin="0,0,0,10"/>

        <TextBox x:Name="UrlTextBox"
                 Grid.Row="1"
                 Text="https://"
                 FontSize="14"
                 Padding="10,8"
                 Background="#333333"
                 Foreground="White"
                 BorderBrush="#555555"
                 CaretBrush="White"/>

        <TextBlock Grid.Row="2"
                   Text="💡 Chrome이 자동으로 디버그 모드로 시작되어 페이지를 캡처합니다."
                   Foreground="#888888"
                   FontSize="11"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="캡처"
                    Click="OkButton_Click"
                    Width="80" Height="32"
                    Background="#0078d4"
                    Foreground="White"
                    BorderThickness="0"
                    Margin="0,0,10,0"/>
            <Button Content="취소"
                    Click="CancelButton_Click"
                    Width="80" Height="32"
                    Background="#333333"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
